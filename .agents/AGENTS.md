# Posit Project Rules

- **Do NOT run `npm run test:all` or full test suites** unless absolutely required. The test suite takes a very long time to complete (approx. 50 minutes). Instead, run target test suites or individual tests (e.g., `npm run test`) to verify specific functionality.
