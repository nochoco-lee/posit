classDiagram
    title User Profile

    class User {
        -Long id
        -String name
        -String email
        -String passwordHash
        -DateTime createdAt
        +getEmail() String
        +verifyPassword(pw String) Boolean
        +updateProfile(name String, email String) void
    }
